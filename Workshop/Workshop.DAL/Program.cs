using System.Linq;
using System.Collections.Generic;
using Workshop.Models.Entities;
using System;

namespace Workshop.DAL
{
    class Program
    {
        static void Main(string[] args)
        {
            /*var context = new WorkshopContext();
            var owner = new Owner()
            {
                Name = "Kakashi",
                Surname = "Hatake",
                PhoneNumber = "222333444",
                Cars = new List<Car>()
                {
                    new Car(){Mark = "renalut", Model = "megane", Registration = "swd333"},
                    new Car(){Mark = "citroen", Model = "c2", Registration = "src333"},
                }
            };

            var car = owner.Cars.First();
            car.Orders = new List<Order>();
            car.Orders.Add(
                new Order()
                {
                    Name = "wymiana oleju",
                    StartDate = DateTime.Now
                }
            );
            context.Add(owner);
            context.SaveChanges();
            Console.WriteLine("dodano");*/
        }
    }
}
